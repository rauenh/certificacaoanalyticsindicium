version: 2

models:

  - name: businessentityid
    description: 'Person identity number, PK FK'
    tests:
      - not_null
  - name: personid
    description: 'FK to person.personid'
    tests:
      - not_null
